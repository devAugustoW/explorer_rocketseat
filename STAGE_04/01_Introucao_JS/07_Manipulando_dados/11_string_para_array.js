// Manipulando Arrays

// Array.from() -> transforma a String em Array e cada letra da String passa a ser um elemento do Array



let word = 'Manipulação'
console.log(word)
console.log(Array.from(word))